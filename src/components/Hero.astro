---

---

<section id="inicio" class="hero">
    <div class="video-background">
        <video
            autoplay
            muted
            loop
            playsinline
            poster="https://winstonandharrystore.com/wp-content/uploads/2025/06/winston-harry-zapatos-de-cuero-colombia.jpg.webp"
        >
            <source
                src="https://winstonandharrystore.com/wp-content/uploads/Winston-Harry-Sin-Limites-comp.mp4"
                type="video/mp4"
            />
        </video>
        <div class="overlay"></div>
    </div>

    <!-- Overlay Image "Eleva tu estilo sin límites" -->
    <img
        src="https://winstonandharrystore.com/wp-content/uploads/eleva-tu-estilo-sin-limites.png"
        alt="Eleva tu estilo sin límites"
        class="hero-overlay-img"
        referrerpolicy="no-referrer"
    />
</section>

<style>
    .hero {
        position: relative;
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        overflow: hidden;
        background-color: #000;
        margin-top: 90px;
    }

    .video-background {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 0;
    }

    .overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.3);
        z-index: 1;
    }

    .video-background video {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .hero-overlay-img {
        position: absolute;
        bottom: 100px;
        left: 50%;
        transform: translateX(-50%);
        z-index: 10;
        width: 80%;
        max-width: 600px;
        opacity: 0;
        animation: fadeInUp 1.5s ease-out forwards;
        animation-delay: 0.5s; /* Pequeño retraso para que cargue primero el video */
    }

    @keyframes fadeInUp {
        0% {
            opacity: 0;
            transform: translate(-50%, 50px);
        }
        100% {
            opacity: 1;
            transform: translate(-50%, 0);
        }
    }

    @media (max-width: 992px) {
        .hero {
            height: auto;
            aspect-ratio: 16 / 9;
            margin-top: 65px;
        }

        .hero-overlay-img {
            max-width: 400px;
            bottom: 20px;
        }
    }

    @media (max-width: 480px) {
        .hero-overlay-img {
            max-width: 160px;
            bottom: 15px;
        }
    }
</style>
