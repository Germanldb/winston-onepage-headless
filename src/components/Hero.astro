---
import { Image } from "astro:assets";
---

<section id="inicio" class="hero">
    <div class="container hero-container">
        <div class="hero-content">
            <span class="sub-title">Artesanía Colombiana</span>
            <h1>Elegancia en cada paso</h1>
            <p>
                Descubre nuestra colección exclusiva de calzado artesanal, hecho
                a mano con los mejores materiales y la tradición de maestros
                zapateros.
            </p>
            <div class="hero-btns">
                <a href="#tienda" class="btn">Explorar Tienda</a>
            </div>
        </div>
        <div class="hero-image">
            <div class="image-wrapper">
                <Image
                    src="https://winstonandharrystore.com/wp-content/uploads/Liverpool-miel-cuero-zapato-winstonandharry-8.jpg"
                    alt="Calzado Artesanal Winston & Harry"
                    width={800}
                    height={1000}
                    class="reveal"
                    referrerpolicy="no-referrer"
                />
            </div>
        </div>
    </div>
</section>

<style>
    .hero {
        height: 100vh;
        display: flex;
        align-items: center;
        background-color: var(--color-white);
        padding-top: 5rem;
    }

    .hero-container {
        display: grid;
        grid-template-columns: 1fr 1fr;
        align-items: center;
        gap: 4rem;
    }

    .sub-title {
        color: var(--color-beige);
        text-transform: uppercase;
        letter-spacing: 3px;
        font-weight: 600;
        font-size: 0.9rem;
        display: block;
        margin-bottom: 1rem;
    }

    h1 {
        font-size: 4.5rem;
        line-height: 1.1;
        margin-bottom: 2rem;
    }

    p {
        font-size: 1.1rem;
        margin-bottom: 3rem;
        color: #555;
        max-width: 500px;
    }

    .image-wrapper {
        position: relative;
        border-radius: 10px;
        overflow: hidden;
        box-shadow: 30px 30px 60px rgba(0, 0, 0, 0.1);
    }

    .image-wrapper img {
        width: 100%;
        height: auto;
        display: block;
    }

    .image-wrapper::after {
        content: "";
        position: absolute;
        top: 20px;
        left: 20px;
        right: -20px;
        bottom: -20px;
        border: 2px solid var(--color-beige);
        z-index: -1;
    }

    @media (max-width: 992px) {
        .hero {
            height: auto;
            padding: 10rem 0 5rem;
        }
        .hero-container {
            grid-template-columns: 1fr;
            text-align: center;
        }
        h1 {
            font-size: 3rem;
        }
        p {
            margin: 0 auto 3rem;
        }
    }
</style>
