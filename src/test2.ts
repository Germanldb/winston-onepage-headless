import { getProductBySlug } from './lib/woocommerce'; getProductBySlug('sueter-braid').then(res => { console.log('RESOLVED MEDIA:', Object.keys(res.wpc_resolved_media || {})); res.variations_data.forEach(v => { const m = v.meta_data?.find((m) => (m.key === 'wpcvi_images' || m.key === 'wd_additional_variation_images_data') && m.value); console.log(v.id, m ? m.value : 'NONE'); }); }).catch(console.error);
